<% if @moved %>
  $currentStep = $('#step-<%= @step.id %>').closest('.step-wrapper');
  $currentStep.insertBefore($currentStep.prev());
  <%= render 'timelines/map', timeline: @step.timeline %>
<% end %>